## Cursor Cloud specific instructions

This repository is currently empty (contains only `README.md`). There are no application services, dependencies, build systems, or test frameworks configured.

### Current state

- **No application code** — no source files in any language.
- **No dependency files** — no `package.json`, `requirements.txt`, `go.mod`, `Cargo.toml`, or similar.
- **No build/test tooling** — no `Makefile`, `Dockerfile`, `docker-compose.yml`, CI config, or test runner.
- **No linting config** — no `.eslintrc`, `pyproject.toml`, `.rubocop.yml`, or similar.

### Development notes

- Once application code is added, update this section with instructions for running services, tests, and linting.
- The update script (VM startup) is a no-op (`echo "No dependencies to install"`) until dependencies are introduced.
